description: 'Catalog for files generated from ICON GSV.  '
sources:
  image:
    description: image files data catalog
    driver: yaml_file_cat
    args:
      path: '{{CATALOG_DIR}}/image/image.yaml'
